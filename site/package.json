{
  "name": "hyperliquid-prime-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "npx serve dist -s -l 0.0.0.0 -p ${PORT:-3000}"
  },
  "dependencies": {
    "astro": "^5.3.0",
    "@tailwindcss/vite": "^4.0.0",
    "serve": "^14.2.0",
    "tailwindcss": "^4.0.0"
  }
}
